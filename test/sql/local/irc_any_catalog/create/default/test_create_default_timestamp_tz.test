# name: test/sql/local/irc_any_catalog/create/default/test_create_default_timestamp_tz.test
# group: [default]

require-env ICEBERG_SERVER_AVAILABLE

require-env SECRETS_CREATED_AND_CATALOG_ATTACHED

require avro

require parquet

require iceberg

require httpfs

require icu

statement ok
SET TimeZone='ECT';

statement ok
SET CALENDAR='gregorian';

# Do not ignore 'HTTP' error messages!
set ignore_error_messages

statement ok
set enable_logging=true

statement ok
set logging_level='debug'

statement ok
CREATE SCHEMA IF NOT EXISTS my_datalake.default;

statement ok
DROP TABLE IF EXISTS my_datalake.default.tbl_types;

# Test TIMESTAMP_TZ default
statement ok
CREATE TABLE my_datalake.default.tbl_types(
	id int,
	timestamp_tz_col TIMESTAMPTZ DEFAULT '2025-09-22T08:00:00Z'::TIMESTAMPTZ
) WITH ('format-version'='3');

statement ok
INSERT INTO my_datalake.default.tbl_types VALUES(1, DEFAULT);

query II
SELECT * FROM my_datalake.default.tbl_types;
----
1	2025-09-22 10:00:00+02

statement ok
DROP TABLE my_datalake.default.tbl_types;
